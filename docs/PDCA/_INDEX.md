# PDCA Index

| Feature | Match Rate | 완료일 | Documents |
|---------|:----------:|--------|-----------|
| [PDCA-workspace](2026-08/PDCA-workspace/) | 87% | 2026-08-07 | plan, design, analysis, report — **최초 사이클**. Workspace>Project>Document 3계층 웹앱. 핵심 설계는 URL 경로 미러링(D-13)으로 링크 resolver 없이 브라우저 상대 URL 해석에 위임 — cogmo-report 실문서 67개 임포트로 링크 4종(형제/타폴더/디렉터리/레포외부) 전부 실증(C6, 이 사이클의 관문). 4세션(module-1~4)에 걸쳐 매 배치 실배포+curl 자체검증을 병행해 로컬 테스트로는 안 잡히는 배포 버그 6건(Vercel alias 미해석, zod 런타임 에러로 API 전체 다운, Vercel Vite 프리셋의 다단 API 경로 404, SPA 폴백이 정적자산 삼킴, postgres 유니크 위반 500, 전역 onError가 인증 401 삼킴)을 그 자리에서 발견·수정. Check 단계 gap-detector 정적분석에서 서버 재정규화 누락(Critical) 발견해 즉시 수정·재실증. Match Rate 87%(Critical 수정 후, Important 4건·Minor 12건은 형 판단으로 백로그) |
| [backlog-with-mcp](2026-08/backlog-with-mcp/) | 96% | 2026-08-07 | plan, design, analysis, report — **2번째 사이클**. 프로젝트별 백로그 보드(상태·중요도·처리일·드래그 정렬·접힘 3섹션) + 원격 MCP 서버(`/api/mcp`, stateless Streamable HTTP, 툴 8개) + PAT 인증. 옵션 B(Clean)로 서비스 계층 도입, 기존 라우트 4개도 함께 추출. `shared/transition.ts` 순수 함수 하나로 서버·MCP·UI의 상태 전이 권한 경계(Q10a·Q10b)를 코드로 강제 — T1~T8 전수 테스트 + 실 MCP 호출로 거부 확인(C8). Check 단계에서 gap-detector 정적분석(정적 3축 93.6%) 결과 중 2건을 git 히스토리 대조로 오탐 확정, 실 DB(`app.request()` 하네스)로 Design §8.2 L1 API 시나리오 13개 전수 재실증(100%). 실사용 중 발견한 이슈 2건 그 자리에서 수정(정렬 SQL 캐스팅 버그, main 브랜치 마이그레이션 미적용) + claude.ai 웹 커넥터 대응으로 MCP 인증에 쿼리파라미터 폴백 추가(D-18). Match Rate 96%, Success Criteria는 3/10 완전충족·5/10 부분(API는 실증, 브라우저 실사용 미확인)·2/10 미충족(C7·C10, 배포 후 형의 실사용 필요). **(사후 2026-08-08, refine-mcp-hardening 사이클로 전부 해소)**: C7·C10은 claude.ai 웹 MCP 커넥터로 완주, 이월했던 Critical(처리일 삭제 불가)·Important(Origin 에러코드)·Minor(MCP 스키마 미재사용)도 module-1·2에서 해소 |
| [cycle-release-note](2026-08/cycle-release-note/) | 50%※ | 2026-08-08 | plan, design, analysis, report — **3번째 사이클, `v0.1.2`. 유일하게 PDCA 없이 구현하고 사후 문서화한 사이클.** 버전(release)을 1급 엔티티로 세워(`cycles` 테이블, version 유일키, 릴리즈노트 마크다운, PDCA 사이클은 선택 연결) 프로젝트 개요에 버전 목록·릴리즈노트 펼침·정렬 3종을 붙이고, 사이클 카드의 plan/design/analysis/report 4버튼으로 문서를 오가게 했다(존재=링크, 부재=경로 프리필 생성폼). 곁들여 임포트 폼을 사이클 중심으로 재설계(경로 자동 조립), 사이드바를 전역 계층 네비로 전환, 여름 팔레트 교체(토큰 이름 유지로 CSS 1파일+상수 1줄), 로컬 상주 Hono 개발 서버로 요청당 ~2초→수백ms. ※**50%는 2차의 96%와 같은 자가 아니다** — 사후 Design은 구현의 사본이라 통상 Gap Analysis가 정의상 100%가 되므로, 기준선을 **프로젝트 규약·2차 회고 Try·실행 검증** 3원천으로 재정의해 측정했다(analysis §1.2). 결과가 이 사이클의 핵심 발견: **구조가 강제하는 축은 절차 없이도 만점**(계층 6/6, 보안·회귀 9/9)인데 **사람의 기억에 의존하던 축은 0점**(2차 회고 Try 0/3, 런타임 실증 0/11). 특히 2차에서 Critical로 잡고 회고·이월까지 시킨 `.optional()` vs `.nullable()` 버그가 다른 컬럼에서 **그대로 재발**(C-1 사이클 연결 해제 불가). Critical 1·Important 5·Minor 10 검출(M-10 깨진 링크는 종료 절차에서 해소, 나머지 15건 이월) |
| [refine-mcp-hardening](2026-08/refine-mcp-hardening/) | 100%※ | 2026-08-08 | plan, design, analysis, report — **4번째 사이클, `v0.1.3`.** backlog-with-mcp가 Checkpoint 5에서 이월한 6건(Critical·Important·보안확인·정합·문서2건)을 닫는 마감 사이클. 새 기능 없음. Plan 단계에서 실측(F11~F27)으로 이월 항목의 실제 원인을 재확정 — 서버 쓰기 계층은 이미 null을 올바르게 처리하고 있어 수정이 필요한 건 zod 스키마 한 줄+UI뿐이었음(F13), MCP 스키마 미재사용 갭이 16곳으로 구체화됨(§1.3.1). Design 단계에서 Plan §6.2 검증 4건을 전건 실측(V1~V4) 후 결정 확정 — 특히 V3로 현행 `openedOn:'aaa'`가 500이 아니라 Postgres raw 에러 누출임을 확인해 개선 목표를 재정의. module-1~2(closedOn nullable+✕버튼, `FORBIDDEN` 코드, MCP 스키마 8툴 재사용) 구현을 실 DB 하네스 36/36 + 브라우저(형 확인)로 완료. module-3에서 `?token=` 프로덕션 로그 잔존을 형이 직접 확인(**남음** — 평문, C13, 완화는 OAuth 승격 근거 기록만) + 웹 커넥터 실사용 4건 확인(C14). module-4에서 2차 사이클 문서 3종·`_INDEX.md` 사후 정정 + 배포 체크리스트(`docs/deploy/CHECKLIST.md`) 신설. ※**Match Rate 대신 Scope Closure Rate 채택** — 마감 사이클이라 Design 대 구현의 구조적 일치율 산식이 스코프와 안 맞아, Plan이 정의한 "스코프 6건이 실측/실증으로 닫혔는지"를 판정 기준으로 썼다(report §1.2). Success Criteria 6/6 완전 충족, Critical·Important 이월 0건 |
| [refine-cycles-hardening](2026-08/refine-cycles-hardening/) | 100%※ | 2026-08-09 | plan, design, analysis, report — **5번째 사이클, `v0.1.4`.** cycle-release-note(3차, 사후 문서화, Match 50%)가 이월한 cycles 도메인 데이터 무결성 3건(C-1 사이클 연결 해제 불가·I-1 pair rule 소실·I-4 런타임 실증 0건)을 닫는 마감 사이클. 새 기능 없음. `.optional()` vs `.nullable()` 결함이 2차 `closedOn`(Critical) → 4차가 해소 → 3차 `cycles.name`/`yearMonth`로 **3번째 재발**한 자리 — 3차가 PDCA 절차 없이 구현해 2차 회고 학습이 다시 읽히지 않았던 게 원인. Plan §1.3.1에서 후보 수정 형태를 사전 실측해 한 형태(`cycleFields.partial().refine(엄격 pairRule)`)가 C-1·I-1을 동시에 닫음을 확인, Design §1.3에서 V1~V5 전건 실측(느슨 술어의 편측 해제 통과 반례 확보, zodResolver 순수 호출로 폼 null 통과 확인, dev DB 부정합 0건, PAT 직접발급+합성 owner 격리 방식 확정). **module 순서를 4차와 반대로(검증→코드) 잡아 하네스가 실제로 결함을 잡는다는 증거를 red(6건 실패)로 먼저 남기고 수정 후 green(15/15)으로 전환**(RK-23 대응) — 이 과정에서 Design의 red 예측(5건)이 놓친 시나리오 종속 관계 1건과 하네스 자체 결함 2건을 실행 중 발견·정정. 형이 Plan v0.1의 "하네스 임시 스크립트" 권고를 반론으로 뒤집어 **저장소 커밋 자산화**(3번째이자 마지막 재건축이 되도록) 채택. ※**Match Rate 대신 Scope Closure Rate 채택**(4차 선례) — Plan §4가 정의한 "스코프 3건이 실측/실증으로 닫혔는지"를 판정 기준으로 썼다. Success Criteria 4/4 완전 충족, Critical·Important 이월 0건. **3차 회고 Try P-1(직전 Try 이행 현황 표)·P-2(zod "지울 수 있는가" 열)·P-3(L1 시나리오 DoD 체크박스) 최초 적용 사례** — 세 형식 전부 이 사이클 문서에서 실제 결함을 잡는 데 쓰였다 |
| [expand-mcp-agency](2026-08/expand-mcp-agency/) | 97% | 2026-08-09 | plan, design, analysis, report — **6번째 사이클, `v0.1.5`.** 4·5차와 달리 **마감이 아닌 기능 사이클**(Match Rate로 판정 복귀, D-40). 클로드(MCP)의 백로그 상태 권한을 2차 Q10b/D-13("해소·삭제만")에서 `doing`·`done`까지 확대하되 **`todo` 복귀는 형 전용으로 남겼다**(D-42) — 실사용에서 5차 완주 시 형이 완료 7건을 수동 전환해야 했던 비용이 근거. `MCP_ALLOWED_TARGETS`를 `shared/transition.ts`에 export해 1·2차 방어(zod enum·`canTransition`)의 허용 집합을 구조적으로 단일화(D-44)했고, "완료/해소"의 의미를 형이 확정한 문안대로 `STATUS_MEANING` 상수로 코드에 박아 툴 description·UI 툴팁이 공유하게 했다(D-45). cycles MCP 읽기 툴 `cycle_list`·`cycle_read`를 신설했으나 **쓰기 툴은 0건으로 유지**(D-46, "한 사이클에 경계는 하나만 옮긴다"). 5차가 남긴 L1 하네스 골격(`cycles.l1.ts`)이 **export 0건**이라 그대로 재사용 불가능했음을 실측으로 발견해(F46) `server/l1-harness.ts`로 추출 후 공유(D-48) — 추출 과정에서 `.l1.ts` 파일이 2개가 되며 vitest 파일 간 병렬 실행이 실 DB에서 경합을 내는 것을 module-1에서 발견·해소(`fileParallelism: false`). 구현 중 발견 1건은 형과 논의해 결정: zod enum이 `MCP_ALLOWED_TARGETS`에서 파생되므로 `to='todo'` 요청은 1차(zod) 방어에서 이미 거부되고 2차(`canTransition`) 방어엔 영영 도달하지 않는다는 것(2차 시점부터 사실이었던 구조) — "현 구조 유지"로 확정. 형이 웹 커넥터로 실사용 5건(완료 전환·되돌리기 거부·의도/우연 갈라찍기·릴리즈노트 요약·재배열) + 브라우저 확인 2건(완료 섹션 반영·배지 툴팁) 전부 성공. `tools/list` 캐싱(8→10개 갱신 지연)도 Design이 미리 지목한 그대로 재현·해소. Success Criteria 5/5 완전 충족, Critical·Important 이월 0건. **RULE.md 종료 절차가 세션 밖에서 개정**된 것을 발견 — "PDCA-workspace MCP로 릴리즈 생성"을 요구하지만 `cycle_create` MCP 툴이 아직 없어(D-46), 이번 릴리즈는 브라우저로 생성하고 그 툴 신설은 다음 사이클 후보로 재이월(5차 D-34 조건부 지시와 병합) |
| [refine-cycle-closing](2026-08/refine-cycle-closing/) | 90%→100%※ | 2026-08-09 | plan, design, analysis, report — **7번째 사이클, `v0.1.6`.** 사이클 종료 시 PDCA 문서를 서버에 반영하는 절차의 두 누수를 닫는 기능 사이클 — ①클로드가 MCP `document_write`를 직접 호출하면 본문을 출력 토큰으로 재타이핑해야 했고(토큰 낭비) ②절차가 "이번 사이클 4종"만 상정해 이전 사이클 사후개정분이 서버에 반영되지 않은 채 최소 2회 새어나갔다(F65). `scripts/docs-upload.ts` 하나로 해결 — 대상은 git 최신 태그 기준 자동 탐지(작업트리∪커밋분), 문서는 MCP `document_write` upsert로, 사이클 생성은 REST로. Checkpoint 3에서 형이 클로드 권고(파서만 분리)를 뒤집고 `scripts/lib/` 3계층 클린 구조를 선택(D-66, 5차 A1형 사례). 형의 use case 확장 지적("이전 사이클 문서 수정도 자동 반영돼야") 하나가 스코프를 "4종 업로더"에서 "변경분 동기화기"로 넓혔다. module-4에서 이전 사이클(`expand-mcp-agency`) 문서를 실제로 수정한 뒤 필터 없이 실행해 자동 포함을 실증(C29, 이 사이클의 핵심 증거). Check 단계 gap-detector 정적분석(Overall 90.0%)에서 Important 3건 발견 — ①409 name/version 충돌 오판 ②네트워크 예외 시 업로드 루프 전체 중단(FR-82 위반, module-3에서 이미 관측했으나 당시 놓쳤던 결함) ③주석과 모순되는 도달 불가능 코드 — 전건 실 dev DB로 재현·수정·재검증해 종결(§8). 부수적으로 이전 6개 사이클 문서 전반의 작성자 표기 오류(`cogmo`, 형의 실제 신원이 아님 — 회사계정 혼입으로 추정)를 발견해 28개 문서 정정(별도 커밋). Success Criteria 5/6 Met, C31(개정 절차로 자기 자신을 닫기)은 이 릴리즈 자체가 그 실증 |
| [adopt-pdcaw-cli](2026-08/adopt-pdcaw-cli/) | 100%※ | 2026-08-09 | plan, design, analysis, report — **8번째 사이클, `v0.1.7`.** 7차가 이 저장소 전용으로 급조한 `scripts/docs-upload.ts`+`scripts/lib/{git-changes,workspace-api}.ts`(+테스트, 548줄)를 걷어내고, 같은 로직을 범용화해 npm에 이미 게시한 외부 패키지 `pdcaw@0.2.0`으로 대체한 정리 사이클. 새 기능 없음. Checkpoint 3에서 "코드 추가 없이 리스크를 완화한다"는 C안(Pragmatic)을 채택 — `.pdcarc.json` 신설(`baseUrl`/`projectId` 커밋)과 RULE.md 각주(`pdcaw` breaking release 시 버전 고정 폴백 명령)만으로 RK-44~46(브레이킹 릴리즈·스캔 범위 확대·base-url 기본값 없음) 3건을 대응, 래퍼 코드는 0줄. Do 단계에서 Design이 가정한 "dev 서버 대상 검증"이 `pdcaw`가 외부 CLI라 이 저장소의 `hns-` 격리 하네스를 재사용할 수 없다는 걸 실행 직전에 발견 — 형이 멱등한 upsert라는 성질을 근거로 프로덕션 실행으로 대체 승인(D-74). `.env.local`에서 `PDCAW_PROJECT_ID`/`PDCAW_BASE_URL`을 실제로 잠깐 지웠다 복원하며 `.pdcarc.json` 단독 해석을 실증(E2). E3(릴리즈 생성)는 임의 버전으로 먼저 만들면 실제 종료 시점 태그와 어긋난다는 이유로 실제 종료 절차(이번 커밋)까지 의도적으로 미룸(7차 C31과 동일 패턴). gap-detector 정적분석(Structural 100%/Functional 95%, 서버 무변경이라 API Contract 제외) + `tsc -b`/`oxlint`/`npm test`(64/64) 무회귀 확인. ※**Match Rate 대신 Scope Closure Rate 채택**(4·5차 선례) — Plan이 정의한 스코프 완료 기준(C32~C35)이 판정축. Success Criteria 4/4 완전 충족, Critical·Important 이월 0건 |
| [release-centric-ui](2026-08/release-centric-ui/) | 97%(정적)/11%(SC)※ | 2026-08-09 | plan, design, analysis, report — **9번째 사이클, `v0.1.8`.** 3차(cycle-release-note)가 데이터 모델에서만 세운 "버전=1급 엔티티"(D-19)를 UI 정보구조까지 관철한 기능 사이클. 릴리즈 상세 라우트(`/r/:version`) 신설로 버전 하나=URL 하나를 만들고 그 페이지를 PDCA 4문서 진입점+릴리즈노트로 삼았다(S1·S2). 사이드바·문서 목록에서 `kind='pdca'`를 걷어내(S3·S4) 3차 analysis가 남긴 이중노출(CRN M-3·I-5)의 주 지점을 해소했다. 삭제는 릴리즈·문서 둘 다 목록이 아니라 상세 페이지 하단으로 이관(S6·S10) — S10은 Plan 승인 과정에서 형이 "1차 백로그 F-2(문서 뷰 삭제 버튼) 편입"이라는 제3안으로 직접 제시해 RK-52(삭제 접근 경로 소실)를 해소했다. Checkpoint 3에서 소비처 2곳 이상만 단일 원천화하는 C안(Pragmatic)을 채택해 `releaseUrl`·`isGeneralDoc` 2개 lib만 신설. **이 사이클의 진짜 핵심은 기능이 아니라 판정 방법론** — Plan §4가 미리 "브라우저 실증 없는 항목은 Met으로 안 친다"고 선언했는데, 형이 Do 단계에서 실제로 E1~E10 개별 실증을 생략하는 라이트 패스를 택하자(피로 표명) 그 원칙을 흔들지 않고 Success Criteria 9건 중 8건을 정직하게 Partial로 기록했다. 정적 축(gap-detector+`tsc`/`oxlint`/`npm test`)은 97%로 매우 높았고 Critical 0건, Check 단계에서 발견한 Important 2건(신규 코드 `!` 단언 6곳·`useCycles` enabled 가드 누락)·Minor 3건을 전부 즉시 수정·재검증했다 — 코드 결함은 0건으로 마감됐지만 SC 판정축(브라우저 실증)에서는 1/9만 Met이라 두 숫자를 분리 표기했다. S9(고아 export `parseCycleStagePath` 제거)도 완료, `git grep` 0건 실증. ※**두 판정축을 병기** — 정적 검증과 Plan이 정의한 실증 기준이 이례적으로 크게 갈린 첫 사례. 3차의 "코드는 완결됐는데 실행 확인이 없다" 패턴이 사전 경고 후에도 재현됐다는 게 회고의 핵심 발견 |
