# PDCA Index

| Feature | Match Rate | 완료일 | Documents |
|---------|:----------:|--------|-----------|
| [PDCA-workspace](2026-08/PDCA-workspace/) | 87% | 2026-08-07 | plan, design, analysis, report — **최초 사이클**. Workspace>Project>Document 3계층 웹앱. 핵심 설계는 URL 경로 미러링(D-13)으로 링크 resolver 없이 브라우저 상대 URL 해석에 위임 — cogmo-report 실문서 67개 임포트로 링크 4종(형제/타폴더/디렉터리/레포외부) 전부 실증(C6, 이 사이클의 관문). 4세션(module-1~4)에 걸쳐 매 배치 실배포+curl 자체검증을 병행해 로컬 테스트로는 안 잡히는 배포 버그 6건(Vercel alias 미해석, zod 런타임 에러로 API 전체 다운, Vercel Vite 프리셋의 다단 API 경로 404, SPA 폴백이 정적자산 삼킴, postgres 유니크 위반 500, 전역 onError가 인증 401 삼킴)을 그 자리에서 발견·수정. Check 단계 gap-detector 정적분석에서 서버 재정규화 누락(Critical) 발견해 즉시 수정·재실증. Match Rate 87%(Critical 수정 후, Important 4건·Minor 12건은 형 판단으로 백로그) |
| [backlog-with-mcp](2026-08/backlog-with-mcp/) | 96% | 2026-08-07 | plan, design, analysis, report — **2번째 사이클**. 프로젝트별 백로그 보드(상태·중요도·처리일·드래그 정렬·접힘 3섹션) + 원격 MCP 서버(`/api/mcp`, stateless Streamable HTTP, 툴 8개) + PAT 인증. 옵션 B(Clean)로 서비스 계층 도입, 기존 라우트 4개도 함께 추출. `shared/transition.ts` 순수 함수 하나로 서버·MCP·UI의 상태 전이 권한 경계(Q10a·Q10b)를 코드로 강제 — T1~T8 전수 테스트 + 실 MCP 호출로 거부 확인(C8). Check 단계에서 gap-detector 정적분석(정적 3축 93.6%) 결과 중 2건을 git 히스토리 대조로 오탐 확정, 실 DB(`app.request()` 하네스)로 Design §8.2 L1 API 시나리오 13개 전수 재실증(100%). 실사용 중 발견한 이슈 2건 그 자리에서 수정(정렬 SQL 캐스팅 버그, main 브랜치 마이그레이션 미적용) + claude.ai 웹 커넥터 대응으로 MCP 인증에 쿼리파라미터 폴백 추가(D-18). Match Rate 96%, Success Criteria는 3/10 완전충족·5/10 부분(API는 실증, 브라우저 실사용 미확인)·2/10 미충족(C7·C10, 배포 후 형의 실사용 필요). **(사후 2026-08-08, refine-mcp-hardening 사이클로 전부 해소)**: C7·C10은 claude.ai 웹 MCP 커넥터로 완주, 이월했던 Critical(처리일 삭제 불가)·Important(Origin 에러코드)·Minor(MCP 스키마 미재사용)도 module-1·2에서 해소 |
| [cycle-release-note](2026-08/cycle-release-note/) | 50%※ | 2026-08-08 | plan, design, analysis, report — **3번째 사이클, `v0.1.2`. 유일하게 PDCA 없이 구현하고 사후 문서화한 사이클.** 버전(release)을 1급 엔티티로 세워(`cycles` 테이블, version 유일키, 릴리즈노트 마크다운, PDCA 사이클은 선택 연결) 프로젝트 개요에 버전 목록·릴리즈노트 펼침·정렬 3종을 붙이고, 사이클 카드의 plan/design/analysis/report 4버튼으로 문서를 오가게 했다(존재=링크, 부재=경로 프리필 생성폼). 곁들여 임포트 폼을 사이클 중심으로 재설계(경로 자동 조립), 사이드바를 전역 계층 네비로 전환, 여름 팔레트 교체(토큰 이름 유지로 CSS 1파일+상수 1줄), 로컬 상주 Hono 개발 서버로 요청당 ~2초→수백ms. ※**50%는 2차의 96%와 같은 자가 아니다** — 사후 Design은 구현의 사본이라 통상 Gap Analysis가 정의상 100%가 되므로, 기준선을 **프로젝트 규약·2차 회고 Try·실행 검증** 3원천으로 재정의해 측정했다(analysis §1.2). 결과가 이 사이클의 핵심 발견: **구조가 강제하는 축은 절차 없이도 만점**(계층 6/6, 보안·회귀 9/9)인데 **사람의 기억에 의존하던 축은 0점**(2차 회고 Try 0/3, 런타임 실증 0/11). 특히 2차에서 Critical로 잡고 회고·이월까지 시킨 `.optional()` vs `.nullable()` 버그가 다른 컬럼에서 **그대로 재발**(C-1 사이클 연결 해제 불가). Critical 1·Important 5·Minor 10 검출(M-10 깨진 링크는 종료 절차에서 해소, 나머지 15건 이월) |
| [refine-mcp-hardening](2026-08/refine-mcp-hardening/) | 100%※ | 2026-08-08 | plan, design, analysis, report — **4번째 사이클, `v0.1.3`.** backlog-with-mcp가 Checkpoint 5에서 이월한 6건(Critical·Important·보안확인·정합·문서2건)을 닫는 마감 사이클. 새 기능 없음. Plan 단계에서 실측(F11~F27)으로 이월 항목의 실제 원인을 재확정 — 서버 쓰기 계층은 이미 null을 올바르게 처리하고 있어 수정이 필요한 건 zod 스키마 한 줄+UI뿐이었음(F13), MCP 스키마 미재사용 갭이 16곳으로 구체화됨(§1.3.1). Design 단계에서 Plan §6.2 검증 4건을 전건 실측(V1~V4) 후 결정 확정 — 특히 V3로 현행 `openedOn:'aaa'`가 500이 아니라 Postgres raw 에러 누출임을 확인해 개선 목표를 재정의. module-1~2(closedOn nullable+✕버튼, `FORBIDDEN` 코드, MCP 스키마 8툴 재사용) 구현을 실 DB 하네스 36/36 + 브라우저(형 확인)로 완료. module-3에서 `?token=` 프로덕션 로그 잔존을 형이 직접 확인(**남음** — 평문, C13, 완화는 OAuth 승격 근거 기록만) + 웹 커넥터 실사용 4건 확인(C14). module-4에서 2차 사이클 문서 3종·`_INDEX.md` 사후 정정 + 배포 체크리스트(`docs/deploy/CHECKLIST.md`) 신설. ※**Match Rate 대신 Scope Closure Rate 채택** — 마감 사이클이라 Design 대 구현의 구조적 일치율 산식이 스코프와 안 맞아, Plan이 정의한 "스코프 6건이 실측/실증으로 닫혔는지"를 판정 기준으로 썼다(report §1.2). Success Criteria 6/6 완전 충족, Critical·Important 이월 0건 |
| [refine-cycles-hardening](2026-08/refine-cycles-hardening/) | 100%※ | 2026-08-09 | plan, design, analysis, report — **5번째 사이클, `v0.1.4`.** cycle-release-note(3차, 사후 문서화, Match 50%)가 이월한 cycles 도메인 데이터 무결성 3건(C-1 사이클 연결 해제 불가·I-1 pair rule 소실·I-4 런타임 실증 0건)을 닫는 마감 사이클. 새 기능 없음. `.optional()` vs `.nullable()` 결함이 2차 `closedOn`(Critical) → 4차가 해소 → 3차 `cycles.name`/`yearMonth`로 **3번째 재발**한 자리 — 3차가 PDCA 절차 없이 구현해 2차 회고 학습이 다시 읽히지 않았던 게 원인. Plan §1.3.1에서 후보 수정 형태를 사전 실측해 한 형태(`cycleFields.partial().refine(엄격 pairRule)`)가 C-1·I-1을 동시에 닫음을 확인, Design §1.3에서 V1~V5 전건 실측(느슨 술어의 편측 해제 통과 반례 확보, zodResolver 순수 호출로 폼 null 통과 확인, dev DB 부정합 0건, PAT 직접발급+합성 owner 격리 방식 확정). **module 순서를 4차와 반대로(검증→코드) 잡아 하네스가 실제로 결함을 잡는다는 증거를 red(6건 실패)로 먼저 남기고 수정 후 green(15/15)으로 전환**(RK-23 대응) — 이 과정에서 Design의 red 예측(5건)이 놓친 시나리오 종속 관계 1건과 하네스 자체 결함 2건을 실행 중 발견·정정. 형이 Plan v0.1의 "하네스 임시 스크립트" 권고를 반론으로 뒤집어 **저장소 커밋 자산화**(3번째이자 마지막 재건축이 되도록) 채택. ※**Match Rate 대신 Scope Closure Rate 채택**(4차 선례) — Plan §4가 정의한 "스코프 3건이 실측/실증으로 닫혔는지"를 판정 기준으로 썼다. Success Criteria 4/4 완전 충족, Critical·Important 이월 0건. **3차 회고 Try P-1(직전 Try 이행 현황 표)·P-2(zod "지울 수 있는가" 열)·P-3(L1 시나리오 DoD 체크박스) 최초 적용 사례** — 세 형식 전부 이 사이클 문서에서 실제 결함을 잡는 데 쓰였다 |
